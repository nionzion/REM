<UserControl x:Class="REM.EstateAgentRepresentation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:REM"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <TextBlock x:Name="Login" Text="{Binding Login}" Margin="10" Width="200" TextWrapping="Wrap" VerticalAlignment="Center"></TextBlock>
            <TextBlock x:Name="Name" Text="{Binding Name}" Margin="10" Width="200" TextWrapping="Wrap" VerticalAlignment="Center"></TextBlock>
            <TextBlock x:Name="Address" Text="{Binding Address}" Margin="10" Width="200" TextWrapping="Wrap" VerticalAlignment="Center"></TextBlock>
            <Button Content="Edit" Click="Button_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="10" Margin="5" Width="100"></Button>
        </StackPanel>
    </Border>
</UserControl>
